Docs:
  - kernel doc for:
      endpoint.c
      match.c
      message.c
      policy.c

External API:
  - actually return compatible/incompatible flags
  - rules for: which unknown items to ignore in userspace?
  - review all different structures of custom ioctls and items if they
    can be somehow unified
  - pids and namespaces, ref struct pid of connection?
  - move monitor to MATCH?

Internal:
  - memfd's file pos is shared, document pread/pwrite
  - conn->ep->bus ==> conn->bus
